# Not currently used -- intent parsing is a local deterministic regex/keyword
# parser (see src/agent/intent_parser.py), not an LLM call. Reserved for the
# optional upgrade path described in that file's docstring, if ever swapped in.
LLM_API_KEY=your_api_key_here
LLM_PROVIDER=groq